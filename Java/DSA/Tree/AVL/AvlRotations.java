package Java.DSA.Tree.AVL;

import Java.DSA.Tree.BinaryTree.TreeNode;

public class AvlRotations {
    public void LLRoation(TreeNode root){
        if(root == null) return;
        TreeNode left,right;
        if(root.left != null)
            left = root.left;
        if(root.right!= null)
            right = root.right;
        
    }
}
